numpy>=2.0
scipy>=1.12
pandas>=2.2
pyarrow>=15
pyyaml>=6
numba>=0.60
matplotlib>=3.8
pytest>=8
torch>=2.2        # optional: torch backend (GPU); CPU wheels are fine for tests
pyahocorasick     # optional; the typist uses a built-in suffix automaton
